# Runtime HLS segments and manifests
hls/

# Logs
logs/

# Codec detection cache
cache/

# Standby video (binary, large)
standby/*.mp4

# Python bytecode
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.swp
*~

# Internal / temp ffmpeg dirs
[internal]/
CACHED
CACHED/
exporting/
resolve/
transferring/
srs/
config/alert.env

# Provider account credentials — resolved into @@ALIAS@@ placeholders at runtime.
# NEVER commit: this maps aliases to real host/user/pass for the IPTV provider.
config/accounts.env
config/tg_menu.json
